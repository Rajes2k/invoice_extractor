# Invoice Extractor — quick deploy

## Local (Windows + VS Code)

1. Open terminal in project root (where `app.py` lives)

2. Create virtualenv and activate:

```powershell
python -m venv venv
.\venv\Scripts\activate